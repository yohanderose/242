#include <stdlib.h>
#include <stdio.h>

int isPrime(int n) {
    int i;
    for (i=2; i<n; i++) {
        if (n%i==0 && i%2!=0) {
            return 0;
        }
    }
    return 1;
}

int main(void) {
    
}
