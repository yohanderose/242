#include <stdio.h>
#include <stdlib.h>

int main() {
  int s1, s2, s3;
  scanf("%d %d %d", &s1, &s2, &s3); /*turn this to a double, scanf shits its pants \n*/
  /*
  if (s1 is the lowest score) {
   print (s2 + s3) / 2
  } else if (s2 is the lowest score) {
   print (s1 + s3) / 2
  } else {
   print (s1 + s2) / 2
  }
  */
  return 0;
}
